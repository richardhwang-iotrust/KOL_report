name: Quarterly KOL Channel Update

# 분기마다 구글시트(SSoT)와 kol_common.py CHANNELS 를 비교해
# 신규(리서치·정보성) 추가 + 랭킹 이탈 삭제를 제안하는 PR 을 자동으로 올린다.
# 실제 반영(머지)은 사람이 PR 리뷰에서 확정한다. (Anthropic API 키 불필요)

on:
  schedule:
    - cron: '0 1 1 1,4,7,10 *'   # 매 분기 첫날 01:00 UTC = 10:00 KST
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: 분기 채널 업데이트 실행
        run: |
          python scripts/update_channels.py \
            --sheet "https://docs.google.com/spreadsheets/d/${{ vars.KOL_SHEET_ID }}/export?format=csv" \
            --apply

      - name: 변경 없으면 종료
        id: diff
        run: |
          if git diff --quiet -- scripts/kol_common.py scripts/kol_excluded.py; then
            echo "changed=false" >> "$GITHUB_OUTPUT"
            echo "이번 분기 변경 사항 없음 — PR 생략."
          else
            echo "changed=true" >> "$GITHUB_OUTPUT"
          fi

      - name: 검토용 PR 생성
        if: steps.diff.outputs.changed == 'true'
        uses: peter-evans/create-pull-request@v6
        with:
          branch: chore/quarterly-kol-update
          title: "chore: 분기 KOL 채널 목록 업데이트"
          commit-message: "chore: 분기 KOL 채널 목록 업데이트 (자동 제안)"
          body-path: channel_update_report.md
          add-paths: |
            scripts/kol_common.py
            scripts/kol_excluded.py
          delete-branch: true

# ── 설정 메모 ──────────────────────────────────────────────────────────────
# 1) 저장소 Variables 에 KOL_SHEET_ID 등록 (Settings → Secrets and variables → Actions → Variables)
#    구글시트 URL 의 /d/ 와 /edit 사이 ID. 시트는 '링크가 있는 모든 사용자 보기 가능'이어야 CSV export 가 열림.
# 2) Settings → Actions → General → "Allow GitHub Actions to create and approve pull requests" 체크.
